# SSH-Brute-Force-Detector
A beginner-friendly security tool written in Python that continuously monitors SSH authentication logs, detects brute-force patterns based on custom thresholds, and automatically mitigates attacks using active firewall rules.
